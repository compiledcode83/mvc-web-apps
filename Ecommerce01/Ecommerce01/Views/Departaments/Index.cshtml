@model IEnumerable<Ecommerce01.Models.Departament>

@{
    ViewBag.Title = "Regioni";
}

<h2>Regioni</h2>

<p>
    @Html.ActionLink("Creare Nuova", "Create", new { }, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edita", "Edit", new { id=item.DepartamentId }, new { @class = "btn btn-warning" }) |
            @Html.ActionLink("Dettagli", "Details", new { id=item.DepartamentId },new { @class = "btn btn-info" }) |
            @Html.ActionLink("Elimina", "Delete", new { id=item.DepartamentId }, new { @class ="btn btn-danger" })
            @*@Html.ActionLink("LinkText", "ActionName", new { id = item.id }, new { @class = "btn btn-success" })*@
        </td>
    </tr>
}
</table>
